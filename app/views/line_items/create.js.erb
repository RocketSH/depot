li = document.querySelector('.carts')
if (li == null) {
  li = document.createElement('div')
  li.classList.add('carts')
}

cart = document.querySelector('.side_nav')
li.innerHTML = "<%= j render(@cart) %>"
cart.appendChild(li)

session_counter = document.querySelector('header>p')
if (session_counter) {
  session_counter.remove()
}